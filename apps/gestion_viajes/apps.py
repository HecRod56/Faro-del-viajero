from django.apps import AppConfig

class GestionViajesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.gestion_viajes' # <--- Asegúrate que diga la ruta completa